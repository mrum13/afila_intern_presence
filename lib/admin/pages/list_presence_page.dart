import 'package:flutter/material.dart';

class ListPresencePage extends StatelessWidget {
  const ListPresencePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}